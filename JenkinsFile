pipeline {
	agent any
	stages {
		stage ("Git") {
			steps {
				git branch: "main",
				url: "https://github.com/CyrilGuissard/JenkinsCodeTest.git"
			}
		}
		stage ("Bandit Test") {
			steps {
				sh "bandit -r $PWD > $WORKSPACE/reports/test.txt"
			}
		}
	}
}
